# Anniversary Gallery Environment Configuration
# Copy this file to .env and modify as needed

# Host port (port on your machine)
# The port you'll access in your browser: http://localhost:HOST_PORT
HOST_PORT=3000

# Container port (port inside Docker container)
# Usually keep this as 3000 unless you have a specific reason to change it
PORT=3000

# Node environment
NODE_ENV=production

# ===== Usage Examples =====
#
# Example 1: Run on port 8080 instead of 3000
# HOST_PORT=8080
# PORT=3000
# Access: http://localhost:8080
#
# Example 2: Run on port 80 (requires sudo/admin)
# HOST_PORT=80
# PORT=3000
# Access: http://localhost
#
# Example 3: Change both ports
# HOST_PORT=5000
# PORT=5000
# Access: http://localhost:5000
